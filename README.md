# POS App (Flutter)
Starter POS application. Copy this project to your machine and run `flutter pub get` then `flutter run`.

Features included:
- Product list & add to cart
- Cart management & checkout
- Local storage with sqflite
- CSV import helper (parsing)
- Simple printer service skeleton for thermal printers